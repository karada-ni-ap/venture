# coding: UTF-8

from food import *

class recipe:
	def __init__(self):
		self.demand = [0] * (nof)
	
	def input_name(self, name):
	  self.name = name

	def input_food(self, food, amount):
	  self.demand[food] = amount

# hoge
